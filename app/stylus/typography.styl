
h1{font-size: 2rem;}

h2{font-size: 1.6rem;}

h3{font-size: 1.4rem;}

h4{font-size: 1.2rem;}

h5{font-size: 1rem;}

h6{font-size: .8rem;}
h1,h2,h3,h4,h5{
    font-weight: bold;
    // font-family: 'Open Sans', Helvetica, sans-serif;
    margin: 1.5rem 0 1rem 0;
    a{
        text-decoration: none;
        color:#000;
        &:hover{
            color:#000;
        }
        &:active{
            color:#000;
        }
    }
}

p{
    // font-family: 'Open Sans', Helvetica, sans-serif;
    font-size: 1rem;
    margin-bottom: 1.3rem;
    color:#333;
    overflow: hidden;
    font-weight: 300;
}
strong{
    font-weight: bold;
}
em{
    font-weight: 500;
}

ul{
    padding-left: 1rem;
    margin: 1rem 0;
    margin-left: 1rem;
    list-style-type: square;
}

body{
    font-family: 'Monaco','Songti TC';
    line-height: 1.5rem;
}

a{
    text-decoration: none;
    color:#333;
}

table{
    width: 100%;
    thead,tbody{
        width: 90%;
        td:first-child{
            width: 70%;
        }
        td:last-child{
            text-align: right;
        }
    }
}